using System;

namespace SuperCalculationLib
{
    public class SuperCalculator
    {
        public double Pow(double root, double power)
        {
            return Math.Pow(root, power);
        }
    }
}
